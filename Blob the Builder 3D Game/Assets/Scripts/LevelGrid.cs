using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelGrid : MonoBehaviour
{
    /*
    private Vector3Int itemGridPosition;
    private int width;
    private int height;
    private int depth;

    public LevelGrid(int width, int height, int depth)
    {
        this.width = width;
        this.height = height;
        this.depth = depth;
    }

    private void SpawnFood()
    {
        itemGridPosition = new Vector3Int(Random.Range(0, width), Random.Range(0, height), Random.Range(0, depth));

        GameObject itemGameObject = new GameObject("Item", typeof(MeshRenderer));
        itemGridPosition.GetComponent<Vector3Int>().mesh = 
    }
    */
}
